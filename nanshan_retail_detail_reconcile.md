# 南山零售财年报表 × 七色米销售明细 — 比对结果

> 生成日期：2026-05-19 ｜ 脚本：[`add_retail_detail_merged_xlsx.py`](add_retail_detail_merged_xlsx.py)

## 1. 数据源与匹配键

| 角色 | 文件 / sheet | 说明 |
|---|---|---|
| 主报表 | `nanshan_retail_orders_fy_2025-05-01_2026-04-30.xlsx` → `年度零售` | 一单一行，56 列，497 单（含关闭） |
| 明细源 | `销售单列表_c393a061-a3c1-4479-9611-f3f577a509c5.xls` → `销售明细单1` | 474 单据 / 595 明细行 |
| 产物 | 主报表新增 sheet `年度零售明细` ＋ 备份 `nanshan_retail_orders_fy_with_detail.xlsx` | 67 列 × 585 数据行 |

**匹配键**：`年度零售.七色米订单号` ＝ `销售明细单1.单据编号`（均 `XSDyyyymmdd0NNX` 格式）。两文件的"关联订单号"列全空，不可用。

## 2. `年度零售明细` sheet 当前形态

- 列结构：原 56 列 ＋ 10 明细列（商品编号/商品名称/商品分类/规格/属性/数量/单价/折扣/折后单价/总额）＋ 末列 **`Σ明细总额−订单结余`**（订单级有符号差额）。
- 一单多商品：纵向展开，订单级 56 列＋末列**合并单元格**跨行（76 单 span≥2）。
- 底色优先级：浅蓝（>1 明细）< 淡粉（差额）< 红（无明细对不上）。
- 表头深蓝 `1F4E78`、freeze A2、金额列 `0.00`。

### 处理规则（按需求逐步固化）

1. `正/闭=关闭` 的单（28 个）**整单从本 sheet 删除**（原 `年度零售` 不动，仍 497 单）。
2. 测试单 `NS_LS_251217_00004`（测试='是'·¥0·未支付）**剔除**。
3. 非关闭、未剔除但无明细（对不上）→ **整行标红 `FF9999`**。
4. 匹配单中 `|Σ明细总额 − 订单结余| > ¥0.01` → **整块淡粉 `FCE4EC`**。

## 3. 正向比对（报表 → 明细）

| 类别 | 单数 | 处理 |
|---|--:|---|
| 匹配上明细 | 465 | 展开 582 明细行，订单级合并 |
| 关闭单 | 28 | 删除（不输出） |
| 测试单 | 1 | 删除（`NS_LS_251217_00004`） |
| 非关闭无明细（标红） | 3 | 保留，整行红 |
| **合计** | **497** | 输出 585 数据行 |

### 金额对账（`年度零售明细` 内）

| 指标 | 全表（465 匹配＋3 红） | 仅 465 匹配单 |
|---|--:|--:|
| Σ总额（col66） | 343,852.00 | 343,852.00 |
| Σ订单结余（col14 去重） | 347,191.00 | 343,004.00 |
| Σ末列（Σ明细−结余） | 848.00 | 848.00 |
| Σ总额 − Σ订单结余 | −3,339.00 | **848.00 ＝ Σ末列 ✓** |

**恒等式 `Σ末列 = Σ总额 − Σ订单结余` 仅在 465 匹配单上精确成立**（差 0.00）。全表对不上的 **¥4,187** 缺口 ＝ 3 个标红无明细单的订单结余之和（关闭单结余全 ¥0、测试单 ¥0，均不影响）。

### 3 个标红单（对不上，¥4,187）

| 订单号 | 业务日期 | 客户 | 订单结余 | 七色米订单号 | 原因 |
|---|---|---|--:|---|---|
| NS_LS_251207_00004 | 2025-12-07 | 杜 | 3,451 | 空 | 无七色米号 |
| NS_LS_251202_00001 | 2025-12-02 | 郑 | 400 | XSD20251202001I | 有号未命中（xls 无此单据） |
| NS_LS_260205_00002 | 2026-02-05 | 张 | 336 | XSD20260205001I | 有号未命中（疑号错一位，见 §5） |

### 2 个淡粉差额单（匹配但 Σ明细≠结余）

| 订单号 | 订单结余 | Σ明细总额 | 末列差额 |
|---|--:|--:|--:|
| NS_LS_260209_00007 | 0 | 718 | +718 |
| NS_LS_260213_00001 | 128 | 258 | +130 |

## 4. 反向比对（明细 → 报表）

`销售单列表.xls` 共 474 个 distinct 单据编号，**6 个不在**报表 `七色米订单号`（471 个非空）中：

| xls 单据编号 | 业务日期 | 客户 | 金额 | 报表同金额单数 | 判定 |
|---|---|---|--:|--:|---|
| **XSD20251207006I** | 2025-12-07 | 1 | 3,451 | 仅 1 | ✅ 唯一金额 → 必为 `NS_LS_251207_00004` 漏录的号 |
| **XSD20260205002I** | 2026-02-05 | 1 | 336 | 14 | ✅ 同日同额 → `NS_LS_260205_00002` 号录错一位（001↔002） |
| XSD20260101007I | 2026-01-01 | 1 | 99 | 16 | ⚠ 唯一空号候选为关闭¥0单；其余各有号 |
| XSD20260117013I | 2026-01-17 | 1 | 199 | 10 | ⚠ 空号候选均关闭¥0；无正常对应 |
| XSD20260205012A | 2026-02-05 | 尹鼎元13851238 | 330 | 3 | ❌ 3 单各有自己七色米号，无对应 |
| XSD20251116001A | 2025-11-16 | 19834679623 | 2,370 | 0 | ❌ 报表里金额都不存在，完全无此单 |

> "报表同金额单数"按金额（不限同日）匹配；常见价位（99/199/336/330）有大量同价但**各自已有七色米号**的无关单，纯金额"存在"噪音大，仅 ¥3,451 为唯一金额、强对应。

## 5. 结论与待办

红色 ¥4,187 缺口拆解：

| 单 | 金额 | 性质 | 建议动作 |
|---|--:|---|---|
| NS_LS_251207_00004 | 3,451 | snowmeet 漏录七色米号；Qisemi 实有 `XSD20251207006I`（唯一金额对应） | 补录七色米号 → 重跑可转正常匹配 |
| NS_LS_260205_00002 | 336 | 号录错一位：报表 `XSD20260205001I`，实为 `XSD20260205002I` | 改正七色米号 → 重跑可命中 |
| NS_LS_251202_00001 | 400 | 报表号 `XSD20251202001I` 在本 xls **完全不存在** | 查 Qisemi 该单据是否在其它导出区间 |

- **可修复 ¥3,787**（前两单补正七色米号即对平），**¥400** 需查 Qisemi 侧导出范围。
- xls 侧 `XSD20251116001A`（¥2,370）报表内任何金额都查无 → 疑非南山店 / 超 25-26 财年窗口 / 未录入 snowmeet，需业务核实。
- 其余 xls 单据（99/199/330 类）报表同价单均已各自正确关联，非漏录。

> 备注：`EXCLUDE_CODES`、`CLOSED_VALUE`、`DIFF_TOL`、各底色常量均在脚本顶部，调整后重跑即可（写入幂等：`年度零售明细` 已存在则删重建，其余 3 sheet 不动；运行前确保主报表未被 Excel 占用）。
